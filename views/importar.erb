<%# views/importar.erb %>
<div class="form-card">
  <div class="form-header">
    <h2>Importar Produtos</h2>
  </div>

  <div class="card-body">
    <p>Selecione um arquivo (CSV, XLSX ou XLS) para adicionar produtos ao seu estoque de uma só vez.</p>
    <hr>
    <form action="/importar" method="post" enctype="multipart/form-data">
      <div class="mb-3">
        <label for="file" class="form-label">Selecione o arquivo:</label>
        <input type="file" class="form-control" id="file" name="file" accept=".csv, .xlsx, .xls" required>
      </div>
      <div class="d-flex justify-content-between mt-4">
        <a href="/produtos" class="btn btn-secondary">Cancelar</a>
        <button type="submit" class="btn btn-primary">Importar Produtos</button>
      </div>
    </form>
  </div>
</div>